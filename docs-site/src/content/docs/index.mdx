---
title: web-style-engine
description: Framework-agnostic style engine for Web UI.
---

import VueDemo from '../../components/VueDemo.vue'
import ReactDemo from '../../components/ReactDemo'

`web-style-engine` is a framework-agnostic style engine for Web UI.

It provides reusable style primitives extracted from `antdv-style` without binding the core package to Vue, React, Solid, antdv, or any specific design system.

## Modules

- Core style engine
- Runtime renderers
- Responsive query and observer helpers
- createStylesCore
- Framework adapters
- Token and CSS variable helpers

## Vue

<VueDemo client:load />

## React

<ReactDemo client:load />
