---
title: Theme runtime
description: Dark/light scoped themes and multiple themes on one page.
---

import ThemeRuntimeExample from '../../../../components/ThemeRuntimeExample'

This example registers a default light theme and a scoped dark theme. Both cards use the same generated classes; the second card changes appearance through `[data-theme="dark"]` CSS variables.

<ThemeRuntimeExample client:load />
